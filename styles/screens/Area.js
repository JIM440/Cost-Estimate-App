import { StyleSheet } from 'react-native';

const Area = StyleSheet.create({
  img: {
    borderRadius: 22,
    width: '100%',
    marginBottom: 10,
  },
  picker: {
    height: 50,
    borderRadius: 8,
    marginBottom: 20,
    width: 60,
    borderColor: 'gray',
  },
  resultsText: {
    fontSize: 16,
  },
});

export default Area;
