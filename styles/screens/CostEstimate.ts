import { StyleSheet } from 'react-native';

const ImageStyle = StyleSheet.create({
  image: {
    width: '100%',
    objectFit: 'cover',
    minHeight: 170,
    borderRadius: 10,
    marginBottom: 16,
  },
});

export default ImageStyle;
